spring:
  config:
    import: "optional:configserver:http://j9c211.p.ssafy.io:62110" #config server uri
  cloud:
    config:
      name: member
      profile: dev

  jpa:
    hibernate:
      ddl-auto: create

encrypt:
  key: ${ssh-encrypt-key}
