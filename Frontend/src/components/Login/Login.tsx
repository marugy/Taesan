import React from 'react';

const Login = () => {
    
    return (
        <div>
            <input type="text"  />
            <input type="text" />
            <button>로그인</button>
        </div>
    );
};

export default Login;