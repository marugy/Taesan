import React from 'react';
import NotFound from 'components/Common/NotFound';
const NotFoundPage = () => {
    return (
        <div>
            <NotFound />
        </div>
    );
};

export default NotFoundPage;